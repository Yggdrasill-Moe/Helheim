这里脚本中的0x109为自定义参数可根据需要修改

脚本可以理解为将字模的二进制码分成一个一个等长的块 并在块的开头添加0x8000(因为游戏会读取4bits作为标记 以后再读到标记就会向后读12bits并认定为压缩长度,首次也算在内)
\x80\x00为写入0x80 0x00 
8是标记0x000为压缩0字节
标记可以是0-f中的任意一个 最好选取字模中没有的作为标记(0的话估计会很蛋疼)

0x109的为CT2的tile模式 设置好宽高 偏移为0 点击横排第二个框  CT2右下角显示的参数（为一个tile的长度，这里设置的宽和高均为23）

code by Littlekey
